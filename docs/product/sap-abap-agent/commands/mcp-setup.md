---
description: |
  (DEPRECATED) Ten cu cua /register-mcp-servers. Dung /register-mcp-servers —
  dang ky MCP servers vao Claude Code. KHONG lan voi /mcp (OAuth Notion).
argument-hint: "[--apply | --json | --skip-env]"
---

# /mcp-setup → da doi ten

Lenh nay **doi ten** thanh:

```text
/register-mcp-servers
```

Ly do: `/mcp-setup` de lan voi `/mcp` (Claude Code = dang nhap OAuth / xem MCP).

Hay chay `/register-mcp-servers`. Chi tiet day du nam trong `commands/register-mcp-servers.md`.

CLI tuong duong (ca 2 ten deu chay):

```bash
mcp-sap-connect register-mcp-servers
mcp-sap-connect mcp-setup
```
