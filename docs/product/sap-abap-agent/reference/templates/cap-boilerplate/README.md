# CAP boilerplate (Node)

Used by skill `sap-scaffold-cap`. Local: `npm install && npx cds watch`.

Optional Event Mesh consumer: copy from
`../rap-events-boilerplate/cap-consumer/srv/event-handler.js`.
